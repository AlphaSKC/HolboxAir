import { Box } from "@mui/material";

export default function ImageDivider() {
    return (
        <Box sx={{
            width: '100%',
            height: '30vh',
            backgroundColor: 'black',
        }}>
            
        </Box>
    );
}